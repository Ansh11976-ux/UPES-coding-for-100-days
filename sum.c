#include <stdio.h>

int main () {
    int num1, num2;

    //input two number seprated by space 
    scanf("%d %d", &num1, &num2);

    //display the sum matching the simple output format
    printf ("sum= %d\n", num1 + num2);

    return 0;
}